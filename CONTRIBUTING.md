# 贡献指南

感谢你考虑为“晓记账”贡献代码或文档。

## 提交问题

- 先搜索现有 Issue，避免重复。
- 描述操作系统、App 版本、复现步骤、预期结果和实际结果。
- 不要公开上传真实账本、`.jizhang` 备份、数据库、包含个人路径的截图或未经检查的诊断日志。
- 安全问题请按 [SECURITY.md](SECURITY.md) 的方式报告。

## 开发与测试

在 `app/` 目录执行：

```bash
flutter pub get
dart format --output=none --set-exit-if-changed lib test
flutter analyze
flutter test
```

涉及 Windows 或 macOS 原生插件时，还应在对应系统完成桌面构建和人工验证。

## 代码要求

- 保持金额、汇率和时间计算的整数精度规则。
- 分类最多两级，历史账目不能因分类停用而损坏。
- 新增或修改数据迁移、备份恢复、金额、汇率和隐私逻辑时必须补充测试。
- 日志不得写入金额、备注、分类名称、备份内容或完整用户文件路径。
- 不引入后台联网、遥测或第三方服务，除非先在技术决策文档中说明并获得项目维护者同意。

## 提交与合并请求

- 一次提交聚焦一个可解释的改动。
- 在 Pull Request 中说明动机、实现、测试结果和对用户数据的影响。
- 贡献内容必须能够按 GPL-3.0 发布。
